# 🚀 Инструкция по деплою на VDS

## Подготовка VDS сервера

### 1. Обновление системы

```bash
sudo apt update
sudo apt upgrade -y
```

### 2. Установка Node.js

```bash
# Установка Node.js 18+ через NodeSource
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# Проверка версии
node --version
npm --version
```

### 3. Установка PM2 (менеджер процессов)

```bash
sudo npm install -g pm2
```

### 4. Установка Nginx

```bash
sudo apt install -y nginx
sudo systemctl enable nginx
sudo systemctl start nginx
```

## Деплой приложения

### 1. Клонирование репозитория

```bash
# Перейдите в директорию для проектов
cd /var/www

# Клонируйте репозиторий
sudo git clone https://github.com/axeev202-glitch/phone-exchange.git
sudo chown -R $USER:$USER phone-exchange
cd phone-exchange
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

```bash
# Скопируйте пример файла
cp env.example .env

# Отредактируйте .env файл
nano .env
```

Заполните значения:
- `PORT=3000` - порт для Node.js
- `HOST=0.0.0.0` - хост
- `DOMAIN=your-domain.com` - ваш домен

### 4. Создание директорий

```bash
# Создайте директорию для логов
mkdir -p logs
mkdir -p data
```

### 5. Запуск через PM2

```bash
# Запуск приложения
pm2 start ecosystem.config.js

# Сохранение конфигурации для автозапуска
pm2 save
pm2 startup
```

### 6. Настройка Nginx

```bash
# Скопируйте конфигурацию
sudo cp nginx.conf /etc/nginx/sites-available/phone-exchange

# Отредактируйте конфигурацию (замените your-domain.com на ваш домен)
sudo nano /etc/nginx/sites-available/phone-exchange

# Создайте симлинк
sudo ln -s /etc/nginx/sites-available/phone-exchange /etc/nginx/sites-enabled/

# Проверьте конфигурацию
sudo nginx -t

# Перезапустите Nginx
sudo systemctl restart nginx
```

### 7. Настройка DNS

Настройте DNS записи для вашего домена:
- **A запись**: `@` → IP адрес вашего VDS
- **A запись**: `www` → IP адрес вашего VDS

## SSL сертификат (HTTPS)

### Установка Certbot

```bash
sudo apt install -y certbot python3-certbot-nginx
```

### Получение SSL сертификата

```bash
sudo certbot --nginx -d your-domain.com -d www.your-domain.com
```

Certbot автоматически обновит конфигурацию Nginx для HTTPS.

## Полезные команды PM2

```bash
# Просмотр статуса
pm2 status

# Просмотр логов
pm2 logs phone-exchange

# Перезапуск
pm2 restart phone-exchange

# Остановка
pm2 stop phone-exchange

# Удаление из автозапуска
pm2 delete phone-exchange
```

## Обновление приложения

```bash
# Перейдите в директорию проекта
cd /var/www/phone-exchange

# Получите последние изменения
git pull origin main

# Установите новые зависимости (если есть)
npm install

# Перезапустите приложение
pm2 restart phone-exchange
```

## Мониторинг

```bash
# Мониторинг в реальном времени
pm2 monit

# Просмотр статистики
pm2 show phone-exchange
```

## Безопасность

1. **Firewall**: Настройте UFW
```bash
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP
sudo ufw allow 443/tcp   # HTTPS
sudo ufw enable
```

2. **Регулярные обновления**: Настройте автоматические обновления безопасности
```bash
sudo apt install -y unattended-upgrades
```

3. **Резервное копирование**: Настройте регулярное резервное копирование директории `data/`

## Решение проблем

### Приложение не запускается

```bash
# Проверьте логи
pm2 logs phone-exchange

# Проверьте, занят ли порт
sudo netstat -tulpn | grep 3000
```

### Nginx не работает

```bash
# Проверьте статус
sudo systemctl status nginx

# Проверьте логи
sudo tail -f /var/log/nginx/error.log

# Проверьте конфигурацию
sudo nginx -t
```

### Проблемы с правами доступа

```bash
# Убедитесь, что у пользователя есть права на директорию
sudo chown -R $USER:$USER /var/www/phone-exchange
```

