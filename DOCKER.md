# 🐳 Деплой с Docker

## Быстрый старт

### 1. Установка Docker и Docker Compose

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install -y docker.io docker-compose
sudo systemctl enable docker
sudo systemctl start docker

# Добавьте пользователя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker $USER
# Выйдите и войдите снова, чтобы изменения вступили в силу
```

### 2. Клонирование репозитория

```bash
git clone https://github.com/axeev202-glitch/phone-exchange.git
cd phone-exchange
```

### 3. Настройка переменных окружения

```bash
# Создайте .env файл
cp env.example .env

# Отредактируйте .env
nano .env
```

Установите ваш домен:
```
DOMAIN=your-domain.com
```

### 4. Создание директорий

```bash
# Создайте необходимые директории
mkdir -p data logs/nginx ssl
```

### 5. Запуск приложения (только HTTP)

```bash
# Сборка и запуск контейнеров
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Проверка статуса
docker-compose ps
```

Приложение будет доступно по адресу `http://your-domain.com` или `http://localhost`

## Настройка HTTPS (SSL)

### Вариант 1: Let's Encrypt (рекомендуется)

#### 1. Установка Certbot на хосте

```bash
sudo apt install -y certbot
```

#### 2. Получение SSL сертификата

```bash
# Остановите контейнеры (чтобы освободить порт 80)
docker-compose down

# Получите сертификат
sudo certbot certonly --standalone -d your-domain.com -d www.your-domain.com

# Сертификаты будут в /etc/letsencrypt/live/your-domain.com/
```

#### 3. Копирование сертификатов

```bash
# Создайте директорию для SSL
mkdir -p ssl

# Скопируйте сертификаты
sudo cp /etc/letsencrypt/live/your-domain.com/fullchain.pem ./ssl/
sudo cp /etc/letsencrypt/live/your-domain.com/privkey.pem ./ssl/
sudo chmod 644 ./ssl/fullchain.pem
sudo chmod 600 ./ssl/privkey.pem
```

#### 4. Включение HTTPS в Nginx

Отредактируйте `docker/nginx/conf.d/default.conf`:

1. Раскомментируйте блок HTTPS сервера
2. Раскомментируйте редирект HTTP → HTTPS в начале файла
3. Или используйте отдельный файл `ssl.conf`

```bash
# Скопируйте SSL конфигурацию
cp docker/nginx/conf.d/ssl.conf docker/nginx/conf.d/ssl-enabled.conf
```

#### 5. Перезапуск контейнеров

```bash
docker-compose up -d
```

### Вариант 2: Самоподписанный сертификат (для тестирования)

```bash
# Создайте самоподписанный сертификат
mkdir -p ssl
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout ssl/privkey.pem \
  -out ssl/fullchain.pem \
  -subj "/C=RU/ST=State/L=City/O=Organization/CN=your-domain.com"
```

⚠️ **Внимание**: Самоподписанные сертификаты вызывают предупреждения в браузере. Используйте только для тестирования!

## Управление контейнерами

### Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Только приложение
docker-compose logs -f app

# Только Nginx
docker-compose logs -f nginx
```

### Перезапуск

```bash
# Перезапуск всех контейнеров
docker-compose restart

# Перезапуск конкретного сервиса
docker-compose restart app
docker-compose restart nginx
```

### Остановка

```bash
# Остановка контейнеров
docker-compose stop

# Остановка и удаление контейнеров
docker-compose down
```

### Обновление приложения

```bash
# Получите последние изменения
git pull origin main

# Пересоберите образ
docker-compose build

# Перезапустите контейнеры
docker-compose up -d
```

## Обновление SSL сертификатов

Let's Encrypt сертификаты действительны 90 дней. Настройте автоматическое обновление:

### 1. Создайте скрипт обновления

```bash
nano update-ssl.sh
```

```bash
#!/bin/bash
# Обновление SSL сертификатов

# Обновление сертификатов
sudo certbot renew

# Копирование новых сертификатов
sudo cp /etc/letsencrypt/live/your-domain.com/fullchain.pem ./ssl/
sudo cp /etc/letsencrypt/live/your-domain.com/privkey.pem ./ssl/
sudo chmod 644 ./ssl/fullchain.pem
sudo chmod 600 ./ssl/privkey.pem

# Перезапуск Nginx
docker-compose restart nginx
```

```bash
chmod +x update-ssl.sh
```

### 2. Настройте cron для автоматического обновления

```bash
# Откройте crontab
crontab -e

# Добавьте строку (проверка дважды в день)
0 0,12 * * * /path/to/phone-exchange/update-ssl.sh >> /var/log/ssl-update.log 2>&1
```

## Мониторинг

### Проверка статуса контейнеров

```bash
docker-compose ps
```

### Использование ресурсов

```bash
docker stats
```

### Проверка здоровья

```bash
# Проверка приложения
curl http://localhost:3000

# Проверка через Nginx
curl http://localhost
curl https://localhost
```

## Резервное копирование

### Резервное копирование данных

```bash
# Создайте бэкап
tar -czf backup-$(date +%Y%m%d).tar.gz data/

# Восстановление
tar -xzf backup-YYYYMMDD.tar.gz
```

### Автоматическое резервное копирование

```bash
# Добавьте в crontab
0 2 * * * cd /path/to/phone-exchange && tar -czf /backups/phone-exchange-$(date +\%Y\%m\%d).tar.gz data/
```

## Решение проблем

### Контейнеры не запускаются

```bash
# Проверьте логи
docker-compose logs

# Проверьте конфигурацию
docker-compose config
```

### Проблемы с портами

```bash
# Проверьте, какие порты заняты
sudo netstat -tulpn | grep -E ':(80|443|3000)'

# Остановите другие сервисы, использующие эти порты
```

### Проблемы с SSL

```bash
# Проверьте права доступа к сертификатам
ls -la ssl/

# Проверьте логи Nginx
docker-compose logs nginx
```

### Пересборка образов

```bash
# Полная пересборка без кэша
docker-compose build --no-cache

# Перезапуск
docker-compose up -d
```

## Производительность

### Оптимизация Docker

```bash
# Очистка неиспользуемых ресурсов
docker system prune -a

# Очистка старых образов
docker image prune -a
```

### Масштабирование

Для увеличения производительности можно запустить несколько экземпляров приложения:

```yaml
# В docker-compose.yml измените:
services:
  app:
    # ...
    deploy:
      replicas: 2
```

Или используйте Docker Swarm или Kubernetes для более сложных сценариев.

