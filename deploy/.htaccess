# Минимальная конфигурация для Hostland
# Hostland автоматически проксирует запросы к Node.js приложению

# Отключаем перезапись для статических файлов
RewriteEngine Off

# Или если нужна перезапись, но без блокировки API:
# RewriteEngine On
# RewriteCond %{REQUEST_FILENAME} -f
# RewriteRule ^ - [L]
# RewriteCond %{REQUEST_FILENAME} -d
# RewriteRule ^ - [L]
