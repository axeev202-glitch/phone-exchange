# 🚀 Быстрый push на GitHub

## Первоначальная настройка (один раз):

### 1. Создайте репозиторий на GitHub:
- Зайдите на https://github.com
- Создайте новый репозиторий (например, `phone-exchange`)
- НЕ добавляйте README, .gitignore и лицензию (они уже есть)

### 2. Настройте git:

```powershell
# Запустите скрипт настройки
.\setup-git.ps1
```

Или вручную:
```powershell
# Инициализация
git init
git branch -M main

# Добавление remote
git remote add origin https://github.com/ВАШ_USERNAME/phone-exchange.git
```

## Быстрый push (одна команда):

```powershell
.\push.ps1 "Описание изменений"
```

Например:
```powershell
.\push.ps1 "Исправлена работа API"
.\push.ps1 "Добавлена поддержка префикса /www"
.\push.ps1 "Обновлена структура файлов"
```

## Или вручную:

```powershell
git add .
git commit -m "Описание изменений"
git push origin main
```

## Что игнорируется (.gitignore):

- `node_modules/` - зависимости
- `data/` - пользовательские данные
- `*.log` - логи
- `.env` - переменные окружения

## Важно:

- Не коммитьте `node_modules/` и `data/`
- Коммитьте только исходный код
- Используйте понятные сообщения коммитов

